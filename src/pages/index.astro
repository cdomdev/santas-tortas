---
import Layout from '../layouts/Layout.astro';

---

<Layout title="Welcome to Astro.">
	<main class="">	  
	</main>	
</Layout>


<style>
.ticket {
  position: relative;
  box-sizing: border-box;
  height: auto;
  margin: 10px auto 0;
  padding: 20px;
  background: #fbfbfb;
  border-radius: 10px; /* Redondeo para las esquinas del contenedor */
  overflow: hidden; /* Oculta cualquier exceso de los pseudo-elementos */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Ajusta la sombra para que sea más suave */
}

/* Curva superior */
.ticket::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 20px; /* Altura de la curva */
  background: linear-gradient(to bottom, #fbfbfb 50%, transparent 100%);
  border-radius: 0 0 50% 50%; /* Redondea los bordes inferiores del pseudo-elemento */
  z-index: 1;
}

/* Curva inferior */
.ticket::after {
  content: "";
  position: absolute;
  bottom: -10px; /* Ajuste para que no haya separación entre la curva y el borde */
  left: 0;
  width: 100%;
  height: 40px; /* Aumenta la altura para una curva más pronunciada */
  background: linear-gradient(to top, #fbfbfb 50%, transparent 100%);
  border-radius: 50% 50% 0 0; /* Redondea los bordes superiores del pseudo-elemento */
  z-index: 1;
}
</style>
